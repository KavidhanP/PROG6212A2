@using PROGA22025.Models
@model ClaimViewModel
@* 
Bootstrap 5 Components
Bootstrap Team. (2024).
Bootstrap 5 Documentation.
Bootstrap. https://getbootstrap.com/docs/5.0/forms/form-control/
Accessed: October 22, 2025
*@

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">Submit New Claim - @ViewBag.LecturerName</h3>
                </div>
                <div class="card-body">
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show">
                            @TempData["ErrorMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <form asp-action="SubmitClaim" method="post" enctype="multipart/form-data">
                        <input type="hidden" asp-for="LecturerId" value="@ViewBag.LecturerId" />

                        <div class="mb-3">
                            <label asp-for="HoursWorked" class="form-label">Hours Worked</label>
                            <input asp-for="HoursWorked" class="form-control" type="number" min="1" max="500" placeholder="Enter hours worked" />
                            <span asp-validation-for="HoursWorked" class="text-danger"></span>
                            <div class="form-text">Enter the total number of hours worked (1-500)</div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="HourlyRate" class="form-label">Hourly Rate (R)</label>
                            <input asp-for="HourlyRate" class="form-control" type="number" step="0.01" min="1" max="5000" placeholder="Enter hourly rate" />
                            <span asp-validation-for="HourlyRate" class="text-danger"></span>
                            <div class="form-text">Enter your hourly rate in Rands (R1-R5000)</div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Notes" class="form-label">Additional Notes (Optional)</label>
                            <textarea asp-for="Notes" class="form-control" rows="4" placeholder="Add any additional information"></textarea>
                            <span asp-validation-for="Notes" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label class="form-label">Upload Supporting Documents</label>
                            <input type="file" name="Documents" multiple class="form-control" accept=".pdf,.docx,.xlsx" />
                            <div class="form-text">
                                <strong>Accepted formats:</strong> PDF, DOCX, XLSX (Max 5MB per file)
                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-lg">
                                Submit Claim
                            </button>
                            <a asp-action="Index" asp-route-lecturerId="@ViewBag.LecturerId" class="btn btn-outline-secondary">
                                Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}